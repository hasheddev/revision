Analysing(Explore and understand data) => Data cleansing => Validating(Correctness check) => Documentation and versioning

Cleansing: check code quality, write data transformations, insert into silver

Documentation: Data flow diagram and Data integration diagram

For ddl copy bronze and replace bronze. with silver.

Metadata columns are acdded by data engineers and do not originate from the source data like date data is loaded, updated, source system, file location

SELECT
	prd_id,
	prd_key,
	REPLACE(SUBSTRING(prd_key, 1, 5), '-', '_') cat_id,
	SUBSTRING(prd_key, 1, LEN(prd_key)) prd_key,
	prd_nm,
	ISNULL(prd_cost, 0) prd_cost,
	prd_line,
	prd_start_dt,
	prd_end_dt
FROM bronze.crm_prd_info

--check cost is NULL or NEGATIVE cost
